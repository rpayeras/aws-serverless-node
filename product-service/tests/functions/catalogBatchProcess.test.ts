describe("Testing catalogBatchProcess", () => {
  test("should be called on receive a message in sqs", () => {});

  test("should create products from the message", () => {});

  test("should send a mail after insert the products", () => {});
});
